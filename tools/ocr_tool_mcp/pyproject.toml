[project]
name = "ocr-tool-mcp"
version = "0.1.0"
description = "OCR Tool MCP Server for AI Agents"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "pillow>=10.0.0",
    "chandra-ocr>=0.1.0",
    "pydantic>=2.0.0",
]

[tool.uv]
dev-dependencies = []
